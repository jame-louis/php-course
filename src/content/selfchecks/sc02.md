---
question: "let 和 const 声明变量有什么区别？"
answer: "let 声明的变量可以重新赋值，const 声明的变量不能重新赋值（常量）。"
explanation: "let 用于可能变化的值（如计数器、临时变量），const 用于不会改变的值（如配置项、DOM元素引用）。使用 const 能防止意外修改，使代码意图更清晰。注意：const 对象的内容仍可修改，只是引用不能变。"
module: "基础入门"
tags: ["变量", "语法"]
relatedLectures: ["lecture02"]
draft: false
---
